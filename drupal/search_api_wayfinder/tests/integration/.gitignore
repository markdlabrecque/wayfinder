# Generated by run.sh (composer create-project + drush site:install) on
# every run; never committed. Same convention as the old worktree's harness,
# which never tracked this directory either (verified: `git ls-files` on
# that worktree returns nothing under tests/integration/drupal-site/).
/drupal-site/
