# Frozen Solr reference fixtures (solr-ref/responses/) are committed because
# the test suite asserts against them and must run without Docker.

# Rust
/target/


# Added by cargo

/target

# Claude Code local state (worktrees, local settings)
/.claude/

# Python bytecode from the solr-ref input generators
__pycache__/

# ignore git worktrees
work/
