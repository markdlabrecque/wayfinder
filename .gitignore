# Solr reference fixtures (solr-ref/responses/, manifest.tsv) are committed:
# the test suite asserts against them, so they must be present without Docker.
# Regenerate with solr-ref/capture.sh.

# Rust
/target/


# Added by cargo

/target
